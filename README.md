js-simple-page-scroller
=======================

Simple page scroller

This is a simple native JavaScript page scroller that uses the iterator pattern to show 3 news per page
It has its own unit tests
